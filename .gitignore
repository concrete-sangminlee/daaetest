.DS_Store
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

.venv/
venv/
ENV/

.env

# state.json is intentionally committed to the repo so notifier state
# (the last-seen cursor) survives reliably between scheduled runs.
# It is NOT ignored. Do not add it back here.


